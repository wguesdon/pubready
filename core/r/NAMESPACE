# Every cross-package call in R/ is namespace-qualified (pkg::fn), so this
# NAMESPACE only needs to export pubready's own functions. Kept simple on
# purpose; regenerate with roxygen2 once function docs are added.
exportPattern("^[^.]")
export("%||%")
