# Data and outputs
*.csv
*.xlsx
*.xls
!reference/**/*.csv
!example/**/*.csv
!example/*.csv
# Optional real-data airway benchmark output (tools/make_examples/make_volcano_airway.R).
# Large; the committed volcano example uses the small synthetic de_results.csv instead.
example/de_results_airway.csv
pubready_output/
pubready_demo/
outputs/
figures/
example_output/
render_output/
test_output/
.determinism.*/

# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata
Rplots.pdf

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
# uv.lock is committed on purpose: pubready is an application, and the lock is
# part of the reproducibility story.

# Local agent settings (machine-specific, not shared)
.claude/settings.local.json

# Personal session work log, kept locally but not published
SESSION_SUMMARY.md

# Product requirements doc, kept local only
PRD.md

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Containers
*.tar
